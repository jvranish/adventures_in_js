function LocalGameState(world, glInfo, coordConverter, localPlayer, inputs) {
  this.world = world;
  this.glInfo = glInfo;
  this.coordConverter = coordConverter;
  this.localPlayer = localPlayer;
  this.inputs = inputs;
}